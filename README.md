# AWS Resource Tracker 🛰️

This is a simple Bash script that helps you automatically track and log AWS cloud resources such as **S3 buckets**, **IAM users**, and **EC2 instances**.

## 📋 Features

- ✅ Lists all available S3 buckets
- ✅ Lists all IAM users
- ✅ Lists all EC2 instances
- ✅ Logs all output to a timestamped log file
- ✅ Uses AWS CLI (assumes you're already authenticated)
- ✅ Can be scheduled with cron for daily/regular audits

---

## 📁 Folder Structure

```bash
aws-resource-tracker/
│
├── aws_resource_tracker.sh         # Main script
├── logs/                           # Log files generated by the script
└── README.md                       # Project documentation
⚙️ Prerequisites
AWS CLI installed and configured

bash
Copy
Edit
aws configure
Permissions to run AWS s3, iam, and ec2 commands

Bash shell environment (e.g. Linux, WSL on Windows, or macOS)

🛠️ Usage
Run the script manually:
bash
Copy
Edit
chmod +x aws_resource_tracker.sh
./aws_resource_tracker.sh
Sample output log:
The script will generate logs under the logs/ directory:

bash
Copy
Edit
logs/aws_resource_tracker_2025-04-14_22-40.log
Each run gets its own timestamped log file.

⏰ Automate with Cron (Optional)
To run this script every day at 10:40 PM, you can set up a cron job:

bash
Copy
Edit
crontab -e
Add this line:

bash
Copy
Edit
40 22 * * * /mnt/c/het_projects/shell_scripting/aws_resource_tracker.sh >> /mnt/c/het_projects/shell_scripting/logs/aws_resource_tracker.log 2>&1
